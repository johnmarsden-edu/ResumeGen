---
interface Props {
    date: string;
    prefix?: string;
}
import { formatDateString } from '../helpers/helpers.js';
const { date, prefix = "" } = Astro.props;
---
<span class="date">{prefix}{formatDateString(date)}</span>